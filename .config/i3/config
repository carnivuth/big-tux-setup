# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4


# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
#font pango:monospace 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 13
#font Hack:style=Italic
#font Hack:style=Bold
#font Hack:style=Bold Italic
font Hack:style=Regular 13
font Hack:style=Regular 13
font Font Awesome 6 Brands,Font Awesome 6 Brands Regular:style=Regular
font Font Awesome 6 Free,Font Awesome 6 Free Solid:style=Solid
font Font Awesome v4 Compatibility,Font Awesome v4 Compatibility Regular:style=Regular
font Font Awesome 6 Free,Font Awesome 6 Free Regular:style=Regular

#remove borders from i3 windows
for_window [class="^.*"] border pixel 0

# Start XDG autostart .desktop files using dex. See also
# https://wiki.archlinux.org/index.php/XDG_Autostart
exec --no-startup-id dex --autostart --environment i3

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
#exec --no-startup-id xss-lock --transfer-sleep-lock -- 'dm-tool lock' --nofork

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
exec --no-startup-id nm-applet
#control audio with pamixer
bindsym XF86AudioRaiseVolume exec --no-startup-id pamixer -i 5
bindsym XF86AudioLowerVolume exec --no-startup-id pamixer -d 5
bindsym XF86AudioMute exec --no-startup-id pamixer -t 
# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod
#detect monitor layout
exec_always --no-startup-id autorandr -c

# start a terminal
bindsym $mod+Return exec --no-startup-id alacritty

# kill focused window
bindsym $mod+q kill

# start rofi (a program launcher)
bindsym $mod+d exec --no-startup-id ~/.config/rofi/launchers/carnilauncher/launcher.sh

#change focus
# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
#bindsym $mod+s layout stacking
#bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"
# set workspace on all monitors
workspace $ws1 output eDP-1 
workspace $ws2 output HDMI-1-0
workspace $ws3 output eDP-1 
workspace $ws4 output HDMI-1-0
workspace $ws5 output eDP-1 
workspace $ws6 output HDMI-1-0
workspace $ws7 output eDP-1 
workspace $ws8 output HDMI-1-0
workspace $ws9 output eDP-1 
workspace $ws10 output HDMI-1-0

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10
# switch to combo workspace
bindsym $mod+KP_1 workspace number $ws1; workspace number $ws2
bindsym $mod+KP_2 workspace number $ws3; workspace number $ws4
bindsym $mod+KP_3 workspace number $ws5; workspace number $ws6
bindsym $mod+KP_4 workspace number $ws7; workspace number $ws8
bindsym $mod+KP_5 workspace number $ws9; workspace number $ws10
# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1; workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2; workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3; workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4; workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5; workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6; workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7; workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8; workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9; workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10; workspace number $ws10
# switch workspaces
bindsym $mod+Mod1+Right workspace next
bindsym $mod+mod1+Left workspace prev
#move window to next/previous container
bindsym $mod+control+Mod1+Right move window to workspace next; workspace next 
bindsym $mod+control+Mod1+Left move window to workspace  prev; workspace prev

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode


        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"
#brightness controls
bindsym XF86MonBrightnessDown exec --no-startup-id  brightnessctl -d intel_backlight s 10%- 
bindsym XF86MonBrightnessUp exec --no-startup-id brightnessctl -d intel_backlight s +10% 

#start polybar
exec_always --no-startup-id  .config/polybar/carnibar/launch.sh
#set background image
exec_always --no-startup-id  "~/.fehbg"
#remove window titlebar
default_border pixel 0
#window gaps settings
set $gaps_value 12
gaps inner $gaps_value
#start picom  compositor
exec_always --no-startup-id picom  --config ~/.config/picom/picom.conf &
#setup tap to click touchpad
exec_always --no-startup-id xinput set-prop "ETPS/2 Elantech Touchpad" "libinput Tapping Enabled" 1
#exec_always --no-startup-id /home/matteo/.local/bin/i3-layouts
set $i3l spiral to workspace $ws1
set $i3l spiral to workspace $ws2
set $i3l spiral to workspace $ws3
set $i3l spiral to workspace $ws4
set $i3l spiral to workspace $ws5
set $i3l spiral to workspace $ws6
set $i3l spiral to workspace $ws7
set $i3l spiral to workspace $ws8
set $i3l spiral to workspace $ws9
set $i3l spiral to workspace $ws10

# keybinding to lock screen
bindsym $mod+l exec --no-startup-id "dm-tool lock "
#start dunst
exec_always --no-startup-id "killall dunst"
exec_always --no-startup-id dunst
# Media player controls
bindsym XF86AudioPlay exec --no-startup-id playerctl play-pause
bindsym XF86AudioNext exec --no-startup-id playerctl next
bindsym XF86AudioPrev exec --no-startup-id playerctl previous
#suspend key binding
#bindsym $mod+s exec suspend.sh
#turn of bluetooth at startup
#exec_always btoff.sh

#launch network manager
bindsym $mod+n exec --no-startup-id networkmanager_dmenu
#run gnome polkit for graphical autentication prompt
exec --no-startup-id /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1 &

#screenshot 
bindsym $mod+Print exec --no-startup-id flameshot screen
#manual screenshot 
bindsym $mod+Shift+Print exec --no-startup-id flameshot gui &
#launch telegram 
bindsym $mod+t exec --no-startup-id telegram-desktop
#launch powermenu
bindsym $mod+p exec --no-startup-id ~/.config/rofi/powermenu/carnipowermenu/powermenu.sh
#launch shortcut
bindsym $mod+o exec --no-startup-id ~/.config/rofi/carniapplets/bin/quicklinks.sh
#lock spotify to workspace 10
for_window [class="Spotify"] move to workspace $ws10
#launch rofi launch projects
bindsym $mod+w exec --no-startup-id ~/.config/rofi/launchers/carnilauncher/launchprojects.sh

